# Sätts automatiskt av Vercel när du kopplar en databas till projektet
# (Storage -> Create Database -> Postgres, se README). Behövs inte lokalt –
# saknas den sparas anmälningar i data/waitlist.json istället.
DATABASE_URL=
